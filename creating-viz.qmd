---
title: "Querying with dbplyr"
author: "Kylie Newcomer"
format: html
editor: visual
---
## Connect to database
```{r}
library(tidyverse)
library(DBI)
library(dbplyr)
library(duckdb)

conn <- DBI::dbConnect(duckdb::duckdb(),
                       dbdir = "database/halibut.db")

DBI::dbListTables(conn)
```

```{r}
halibut_db <- tbl(conn, "Halibut")
halibut_db
```

```{r}
trawl_db <- tbl(conn, "Trawl")
trawl_db
```

```{r}
site_db <- tbl(conn, "Site")
site_db
```

```{r}
halibut_length <- halibut_db %>% 
  left_join(trawl_db, by = join_by(trawl_id)) %>% 
  left_join(site_db, by = join_by(loc == location_abbreviation)) %>% 
  filter(loc != "LH" & loc != "T" & loc != "HB" & loc != "M") %>% 
  collect()
```


```{r}
halibut_length %>% 
  filter(season != "Summer" & loc != "SO" & loc != "DH") %>% 
  group_by(location, season) %>% 
  mutate(avg_length = mean(halibut_total_length_mm)) %>% 
  #mutate(location = reorder(location.x, avg_length)) %>% 
  ggplot(aes(halibut_total_length_mm, reorder(location, avg_length), fill = season)) +
  geom_boxplot(color = "black") +
  scale_fill_manual(values = c("#CC5600", "#9CAF88")) +
  labs(x = "Halibut length (mm)",
       y = "",
       fill = "Season",
       title= "On average, larger halibut were caught in the spring",
       subtitle= "The northern most sites (Santa Barbara and Carpenteria) had the largest\naverage lengths") +
  theme_light() +
  theme(panel.grid = element_line(color = "gray"),
        axis.text = element_text(color = "gray1", size = 12),
        #text = element_text(color = "white"),
        axis.title = element_text(size = 14))
  #facet_wrap(~season, ncol = 1)
ggsave(here::here("figs", "halibut_size.png"), height = 4.5, width = 8)
```
```{r}
halibut_db %>% 
  left_join(trawl_db, by = join_by(trawl_id)) %>% 
  left_join(site_db, by = join_by(loc == location_abbreviation)) %>% 
  filter(loc != "LH" & loc != "T" & loc != "HB" & loc != "M") %>% 
  filter(season != "Summer" & loc != "SO" & loc != "DH") %>% 
  group_by(location, season) %>% 
  mutate(avg_length = mean(halibut_total_length_mm)) %>% 
  show_query()
```


```{r}
DBI::dbDisconnect(conn, shutdown = TRUE)
```

